# 提示词冻结归档

这里的每个 `v*` 目录都是当时的完整快照。

- **禁止修改、禁止覆盖、禁止删除。**
- 新版本只能新增目录。用 `python3 scripts/snapshot_prompt.py`。
- 对照原件：`v0.2.0_2026-09-21_user-original-1-to-7/`（你贴出的一至七条）。
- `v0.1.0_*` 是 HTML 反推稿，不能当成你的原文。
- 收盘报告快照用 `close-v*` 目录，与早报 `v*` 分开，同样禁止修改。
